// Authored by: prid1306
#include <iostream>
#include <cmath>
#include <iomanip>
#define FASTIO cin.tie(0)->ios::sync_with_stdio(0)
using namespace std;
int main(){
    long long line;
    cin>>line;
    cout<<setprecision(20)<<4*sqrt(line);
}